mode: Mainline

branches:
  main:
    tag: ''
    increment: Patch
    prevent-increment-of-merged-branch-version: false
    track-merge-target: true

  feature:
    tag: beta
    increment: Minor

  major:
    tag: ''
    increment: Major

ignore:
  sha: []
